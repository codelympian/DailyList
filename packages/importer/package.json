{
  "name": "@dailylist/importer",
  "version": "0.1.0",
  "private": true,
  "description": "Import engine: column detection, normalization, validation, duplicate detection, and staged execution. Shared by API (inline) and worker (queued).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "jest"
  },
  "dependencies": {
    "@dailylist/database": "*",
    "@dailylist/validation": "*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^24",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.9.2"
  }
}
